import fs from "node:fs";

const path = "scripts/test-learning-policy.ts";
let text = fs.readFileSync(path, "utf8");

const availabilityBefore = 'await run("active lesson availability requires both unlock and controlled content", () => { const state = createInitialLearnerState(); assert.equal(isCategoryAvailableForActiveLesson("reading_units", state), true); state.skills.reading_units = { ...state.skills.reading_units, level: "mastery" }; assert.equal(isCategoryUnlocked("vowels_sukun", state), true); assert.equal(isCategoryAvailableForActiveLesson("vowels_sukun", state), true); state.skills.vowels_sukun = { ...state.skills.vowels_sukun, level: "mastery" }; assert.equal(isCategoryUnlocked("shaddah", state), true); assert.equal(isCategoryAvailableForActiveLesson("shaddah", state), true); state.skills.shaddah = { ...state.skills.shaddah, level: "mastery" }; assert.equal(isCategoryUnlocked("article_al", state), true); assert.equal(isCategoryAvailableForActiveLesson("article_al", state), true); state.skills.article_al = { ...state.skills.article_al, level: "mastery" }; assert.equal(isCategoryUnlocked("linking", state), true); assert.equal(isCategoryAvailableForActiveLesson("linking", state), false); });';
const availabilityAfter = 'await run("active lesson availability requires both unlock and controlled content", () => { const state = createInitialLearnerState(); assert.equal(isCategoryAvailableForActiveLesson("reading_units", state), true); state.skills.reading_units = { ...state.skills.reading_units, level: "mastery" }; assert.equal(isCategoryUnlocked("vowels_sukun", state), true); assert.equal(isCategoryAvailableForActiveLesson("vowels_sukun", state), true); state.skills.vowels_sukun = { ...state.skills.vowels_sukun, level: "mastery" }; assert.equal(isCategoryUnlocked("shaddah", state), true); assert.equal(isCategoryAvailableForActiveLesson("shaddah", state), true); state.skills.shaddah = { ...state.skills.shaddah, level: "mastery" }; assert.equal(isCategoryUnlocked("article_al", state), true); assert.equal(isCategoryAvailableForActiveLesson("article_al", state), true); state.skills.article_al = { ...state.skills.article_al, level: "mastery" }; assert.equal(isCategoryUnlocked("linking", state), true); assert.equal(isCategoryAvailableForActiveLesson("linking", state), true); state.skills.linking = { ...state.skills.linking, level: "mastery" }; assert.equal(isCategoryUnlocked("fluent_reading", state), true); assert.equal(isCategoryAvailableForActiveLesson("fluent_reading", state), false); });';

const skipBefore = 'await run("review planning skips an unlocked category with no active session", () => { const state = createInitialLearnerState(); state.skills.reading_units = { ...state.skills.reading_units, level: "mastery", stableAcrossContexts: true, delayedCheckPassed: true }; state.skills.vowels_sukun = { ...state.skills.vowels_sukun, level: "mastery", stableAcrossContexts: true, delayedCheckPassed: true }; state.skills.shaddah = { ...state.skills.shaddah, level: "mastery", stableAcrossContexts: true, delayedCheckPassed: true }; state.skills.article_al = { ...state.skills.article_al, level: "mastery", stableAcrossContexts: true, delayedCheckPassed: true }; state.skills.linking = { ...state.skills.linking, level: "progression", stableAcrossContexts: true, nextReviewAt: "2026-08-24T08:00:00.000Z" }; state.attempts = [attempt("linking", "LINKING-B02-S01", "2026-08-23T20:00:00.000Z", "correct")]; const plan = buildReviewPlan(state, new Date("2026-08-24T20:00:00.000Z")); assert.notEqual(plan.category, "linking"); });';
const skipAfter = 'await run("review planning skips an unlocked category with no active session", () => { const state = createInitialLearnerState(); state.skills.reading_units = { ...state.skills.reading_units, level: "mastery", stableAcrossContexts: true, delayedCheckPassed: true }; state.skills.vowels_sukun = { ...state.skills.vowels_sukun, level: "mastery", stableAcrossContexts: true, delayedCheckPassed: true }; state.skills.shaddah = { ...state.skills.shaddah, level: "mastery", stableAcrossContexts: true, delayedCheckPassed: true }; state.skills.article_al = { ...state.skills.article_al, level: "mastery", stableAcrossContexts: true, delayedCheckPassed: true }; state.skills.linking = { ...state.skills.linking, level: "mastery", stableAcrossContexts: true, delayedCheckPassed: true }; state.skills.fluent_reading = { ...state.skills.fluent_reading, level: "progression", stableAcrossContexts: true, nextReviewAt: "2026-08-24T08:00:00.000Z" }; state.attempts = [attempt("fluent_reading", "FLUENT_READING-B02-S01", "2026-08-23T20:00:00.000Z", "correct")]; const plan = buildReviewPlan(state, new Date("2026-08-24T20:00:00.000Z")); assert.notEqual(plan.category, "fluent_reading"); });';

const articleReview = 'await run("review planning can target an active article-al session after shaddah mastery", () => { const state = createInitialLearnerState(); state.skills.reading_units = { ...state.skills.reading_units, level: "mastery", stableAcrossContexts: true, delayedCheckPassed: true }; state.skills.vowels_sukun = { ...state.skills.vowels_sukun, level: "mastery", stableAcrossContexts: true, delayedCheckPassed: true }; state.skills.shaddah = { ...state.skills.shaddah, level: "mastery", stableAcrossContexts: true, delayedCheckPassed: true }; state.attempts = [attempt("article_al", "ARTICLE_AL-B02-S01", "2026-08-24T08:00:00.000Z", "incorrect", 1), attempt("article_al", "ARTICLE_AL-B02-S01", "2026-08-24T09:00:00.000Z", "incorrect", 2)]; state.skills.article_al = deriveSkillState("article_al", state.attempts); const plan = buildReviewPlan(state, new Date("2026-08-24T20:00:00.000Z")); assert.equal(plan.category, "article_al"); assert.equal(plan.targetSessionId, "ARTICLE_AL-B02-S01"); });';
const linkingReview = 'await run("review planning can target an active linking session after article-al mastery", () => { const state = createInitialLearnerState(); state.skills.reading_units = { ...state.skills.reading_units, level: "mastery", stableAcrossContexts: true, delayedCheckPassed: true }; state.skills.vowels_sukun = { ...state.skills.vowels_sukun, level: "mastery", stableAcrossContexts: true, delayedCheckPassed: true }; state.skills.shaddah = { ...state.skills.shaddah, level: "mastery", stableAcrossContexts: true, delayedCheckPassed: true }; state.skills.article_al = { ...state.skills.article_al, level: "mastery", stableAcrossContexts: true, delayedCheckPassed: true }; state.attempts = [attempt("linking", "LINKING-B02-S01", "2026-08-24T08:00:00.000Z", "incorrect", 1), attempt("linking", "LINKING-B02-S01", "2026-08-24T09:00:00.000Z", "incorrect", 2)]; state.skills.linking = deriveSkillState("linking", state.attempts); const plan = buildReviewPlan(state, new Date("2026-08-24T20:00:00.000Z")); assert.equal(plan.category, "linking"); assert.equal(plan.targetSessionId, "LINKING-B02-S01"); });';

if (text.includes(availabilityAfter) && text.includes(skipAfter) && text.includes(linkingReview)) {
  console.log("OK: linking test fixtures already updated.");
  process.exit(0);
}
if (!text.includes(availabilityBefore)) throw new Error("Availability fixture changed.");
text = text.replace(availabilityBefore, availabilityAfter);
if (!text.includes(skipBefore)) throw new Error("Inactive-category review fixture changed.");
text = text.replace(skipBefore, skipAfter);
if (!text.includes(articleReview)) throw new Error("Article-al review fixture changed.");
text = text.replace(articleReview, `${articleReview}\n${linkingReview}`);
fs.writeFileSync(path, text, "utf8");
console.log("OK: linking learning-policy fixtures updated.");
