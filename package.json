{
  "name": "itqan",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "generate:attempt-registry": "node scripts/generate-attempt-registry.mjs",
    "dev": "node scripts/generate-attempt-registry.mjs && vite",
    "build": "node scripts/generate-attempt-registry.mjs && tsc -b && vite build",
    "test:learning": "node scripts/generate-attempt-registry.mjs && npx --yes tsx scripts/test-learning-policy.ts && npx --yes tsx scripts/test-revision-maintenance.ts && npx --yes tsx scripts/test-progress-insights.ts",
    "test:mastery": "node scripts/generate-attempt-registry.mjs && npx --yes tsx scripts/test-mastery-safety.ts",
    "test:persistence": "node scripts/generate-attempt-registry.mjs && npx --yes tsx scripts/test-persistence.ts",
    "test:session-engine": "node scripts/generate-attempt-registry.mjs && npx --yes tsx scripts/test-session-engine-safety.ts && npx --yes tsx scripts/test-runtime-state-reconciliation.ts && npx --yes tsx scripts/test-active-session-selection.ts",
    "test:interaction-modes": "npx --yes tsx scripts/test-interaction-mode-coverage.ts",
    "test:worker": "npx --yes tsx scripts/test-voice-worker.ts",
    "preview": "vite preview",
    "cf:check": "npm run build && npx --yes wrangler@4 deploy --dry-run",
    "cf:dev": "npm run build && npx --yes wrangler@4 dev",
    "cf:deploy": "npm run build && npx --yes wrangler@4 deploy"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.11",
    "typescript": "^5.7.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "lucide-react": "^0.468.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.3",
    "@types/react-dom": "^19.0.2"
  }
}
